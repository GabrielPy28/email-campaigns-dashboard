import { CreadoresPage } from "./CreadoresPage";

/** Misma UI y flujos que Creadores, contra `creadores-test` / `listas-test`; copy orientado a QA de plantillas e inbox. */
export function PruebasPage() {
  return <CreadoresPage mode="pruebas" />;
}
